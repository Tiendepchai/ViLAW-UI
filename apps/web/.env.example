# URL của proxy server (repo này) – mặc định: http://localhost:8787
VITE_BACKEND_URL=http://localhost:8787

# server: lưu hội thoại vào file JSON qua /conversations
# local : lưu trong localStorage
VITE_STORAGE_MODE=server

# top_k mặc định (số đoạn trích dẫn)
VITE_TOP_K=8
